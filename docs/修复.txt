StockTrader v3 修复与旧代码清理计划                    pt

 状态: 旧文件删除阶段已完成 (2026-05-15)，所有 v1 RL 代码已移除。
       Phase 1-4 的代码修复尚未执行。

 Context

 基于 5 个审查代理对 ~80 个文件的全量审计，发现 ~180 个问题。用户决定删除旧版 v2 纯 RL 训练逻辑，保留数据管理和 v3
 训练代码，然后修复 v3 代码库中的崩溃级和架构级问题。

 旧文件删除清单

 可安全删除（无前端依赖或仅被其他待删文件引用）

 ┌─────────────────────────────────────────┬──────────────────────────────────────┬────────────────────────────────┐
 │                删除文件                 │                 原因                 │              影响              │
 ├─────────────────────────────────────────┼──────────────────────────────────────┼────────────────────────────────┤
 │ rl_agent.py                             │ 旧版RL                               │ frontend/pages/3_AI交易.py     │
 │                                         │ agent，使用SB3直接调用，非v3架构     │ 依赖它                         │
 ├─────────────────────────────────────────┼──────────────────────────────────────┼────────────────────────────────┤
 │ rl_trading_env.py                       │ 仅被 rl_agent.py 依赖                │ 无其他引用                     │
 ├─────────────────────────────────────────┼──────────────────────────────────────┼────────────────────────────────┤
 │ train_rl.py                             │ 旧版RL训练入口脚本，0处import引用    │ 无影响                         │
 ├─────────────────────────────────────────┼──────────────────────────────────────┼────────────────────────────────┤
 │ train_signal.py                         │ 旧版信号训练入口脚本，0处import引用  │ 无影响                         │
 ├─────────────────────────────────────────┼──────────────────────────────────────┼────────────────────────────────┤
 │ run_backtest.py                         │ 旧版回测入口脚本，0处import引用      │ 无影响                         │
 ├─────────────────────────────────────────┼──────────────────────────────────────┼────────────────────────────────┤
 │ data_validator.py                       │ 仅被 3_AI交易.py 使用                │ 随该页面一起删除               │
 ├─────────────────────────────────────────┼──────────────────────────────────────┼────────────────────────────────┤
 │ frontend/pages/3_AI交易.py              │ 旧版RL交易页面，用户确认删除         │ v3 的 9_交易执行.py 可替代     │
 ├─────────────────────────────────────────┼──────────────────────────────────────┼────────────────────────────────┤
 │ data_engine.py                          │ v1 数据引擎，仅被 predictor.py 使用  │ 见下方 predictor 决策          │
 ├─────────────────────────────────────────┼──────────────────────────────────────┼────────────────────────────────┤
 │ src/data_engine.py                      │ 死代码，0处import引用                │ 无影响                         │
 ├─────────────────────────────────────────┼──────────────────────────────────────┼────────────────────────────────┤
 │ layers/models/signal/signal_pipeline.py │ 旧v2                                 │ 仅被 train_signal/run_backtest │
 │                                         │ SignalPipeline，绕过BaseAlphaModel   │  引用                          │
 ├─────────────────────────────────────────┼──────────────────────────────────────┼────────────────────────────────┤
 │ layers/models/signal/mlp_model.py       │ 旧v2 MLP，不继承BaseAlphaModel       │ frontend/pages/6_模型训练.py   │
 │                                         │                                      │ 动态导入                       │
 ├─────────────────────────────────────────┼──────────────────────────────────────┼────────────────────────────────┤
 │ layers/models/signal/__init__.py        │ 旧v2 signal包初始化                  │ 仅导出 SignalPipeline          │
 ├─────────────────────────────────────────┼──────────────────────────────────────┼────────────────────────────────┤
 │ layers/models/rl/rl_executor.py         │ 旧v2 RL executor                     │ 仅被旧脚本引用                 │
 ├─────────────────────────────────────────┼──────────────────────────────────────┼────────────────────────────────┤
 │ layers/models/risk/risk_manager.py      │ 旧v2 RiskManager                     │ 仅被旧脚本引用                 │
 └─────────────────────────────────────────┴──────────────────────────────────────┴────────────────────────────────┘

 保留（有前端直接依赖）

 ┌─────────────────────────┬─────────────────────────────────────────────┐
 │        保留文件         │                    原因                     │
 ├─────────────────────────┼─────────────────────────────────────────────┤
 │ improved_data_engine.py │ 被 5 个前端页面 + app.py + 多个工具脚本使用 │
 ├─────────────────────────┼─────────────────────────────────────────────┤
 │ macd_strategy.py        │ 被 frontend/pages/5_短线交易.py 使用        │
 ├─────────────────────────┼─────────────────────────────────────────────┤
 │ predictor.py            │ 被 frontend/app.py 首页使用                 │
 ├─────────────────────────┼─────────────────────────────────────────────┤
 │ advanced_predictor.py   │ 被 frontend/pages/2_预测分析.py 使用        │
 └─────────────────────────┴─────────────────────────────────────────────┘

 用户已确认的决策

 - 决策 1: 直接删除 frontend/pages/3_AI交易.py（v3 的 9_交易执行.py 可替代其功能）
 - 决策 2: 短期保留 predictor.py + advanced_predictor.py（后续 Phase 再迁移）
 - 决策 3: 删除 data_engine.py (v1)，让 predictor.py 改用 improved_data_engine.py

 v3 代码库修复计划（按优先级分 4 个 Phase）

 Phase 1: 崩溃级修复（5 项，必须先修）

 ┌─────┬─────────────────────────────────────┬──────────────────────────┬─────────────────────────────────────────┐
 │ ID  │                问题                 │           文件           │                修复方式                 │
 ├─────┼─────────────────────────────────────┼──────────────────────────┼─────────────────────────────────────────┤
 │ P1. │ layers/risk/__init__.py             │ layers/risk/__init__.py  │ 新建文件，导出核心类                    │
 │ 1   │ 缺失，import失败                    │                          │                                         │
 ├─────┼─────────────────────────────────────┼──────────────────────────┼─────────────────────────────────────────┤
 │ P1. │ StandardScaler                      │ frontend/pages/6_模型训  │ 将 StandardScaler 导入移到文件顶部      │
 │ 2   │ 作用域Bug，Tab3直接访问NameError    │ 练.py                    │                                         │
 ├─────┼─────────────────────────────────────┼──────────────────────────┼─────────────────────────────────────────┤
 │ P1. │ 风控页面属性赋值不生效              │ frontend/pages/8_风控系  │ 改为 risk_engine.drawdown.stop_loss =   │
 │ 3   │                                     │ 统.py                    │ ... 等正确路径                          │
 ├─────┼─────────────────────────────────────┼──────────────────────────┼─────────────────────────────────────────┤
 │ P1. │ 事件dataclass自定义__init__破坏字段 │ layers/backtest/event.py │ 删除自定义__init__，使用dataclass默认__ │
 │ 4   │                                     │                          │ init__                                  │
 ├─────┼─────────────────────────────────────┼──────────────────────────┼─────────────────────────────────────────┤
 │ P1. │ GRU/TCN/Transformer的history字典未  │ 3个模型文件              │ 已在前面修复 ✅                         │
 │ 5   │ 初始化 → NameError                  │                          │                                         │
 └─────┴─────────────────────────────────────┴──────────────────────────┴─────────────────────────────────────────┘

 Phase 2: 架构断裂修复（8 项，让核心设计真正工作）

 ┌─────┬───────────────────────────┬──────────────────────────────────────────┬──────────────────────────────────┐
 │ ID  │           问题            │                   文件                   │             修复方式             │
 ├─────┼───────────────────────────┼──────────────────────────────────────────┼──────────────────────────────────┤
 │ P2. │ FeatureRegistry           │ layers/features/feature_pipeline.py +    │ 在每个特征组 add() 末尾调用      │
 │ 1   │ 永远为空，特征从未注册    │ 6个特征组文件                            │ registry.register() 注册所有列   │
 ├─────┼───────────────────────────┼──────────────────────────────────────────┼──────────────────────────────────┤
 │ P2. │ vol_change                │ layers/features/features/volatility_feat │ 给至少一方改名，如               │
 │ 2   │ 列名冲突（Volatility vs   │ ures.py 或 volume_features.py            │ vol_regime_change                │
 │     │ Volume）                  │                                          │                                  │
 ├─────┼───────────────────────────┼──────────────────────────────────────────┼──────────────────────────────────┤
 │ P2. │ RiskFeatures 未传入 bench │ layers/features/feature_pipeline.py      │ 传入benchmark_data参数           │
 │ 3   │ mark_data，beta永远=1.0   │                                          │                                  │
 ├─────┼───────────────────────────┼──────────────────────────────────────────┼──────────────────────────────────┤
 │ P2. │ LabelBuilder vs           │                                          │ 统一使用 signals层的             │
 │ 4   │ LabelFactory 双轨并存     │ 前端 + run_pipeline.py                   │ LabelFactory，弃用 features层的  │
 │     │                           │                                          │ LabelBuilder                     │
 ├─────┼───────────────────────────┼──────────────────────────────────────────┼──────────────────────────────────┤
 │ P2. │ _estimate_confidence()    │                                          │                                  │
 │ 5   │ 将预测值而非原始特征喂给  │ layers/ensemble/base_ensemble.py         │ 修改签名，接受X参数              │
 │     │ 子模型                    │                                          │                                  │
 ├─────┼───────────────────────────┼──────────────────────────────────────────┼──────────────────────────────────┤
 │ P2. │ FeatureCache              │ layers/features/feature_cache.py         │ 加入数据内容的md5 + 实际日期范围 │
 │ 6   │ 缓存键不含数据哈希        │                                          │                                  │
 ├─────┼───────────────────────────┼──────────────────────────────────────────┼──────────────────────────────────┤
 │ P2. │ BacktestEngine._build_rl_ │ layers/backtest/backtest_engine.py       │ 填充完整的观测向量，与ExecutionE │
 │ 7   │ obs() 40维只填7维         │                                          │ nv对齐                           │
 ├─────┼───────────────────────────┼──────────────────────────────────────────┼──────────────────────────────────┤
 │     │                           │                                          │ 基于旧版 train_mlp 创建实现      │
 │ P2. │ MLP 模型缺少 v3 接口（不  │ 新建 layers/signals/models/mlp_model.py  │ BaseAlphaModel 的                │
 │ 8   │ 继承BaseAlphaModel）      │                                          │ MLPSignalModel，删除 layers/mode │
 │     │                           │                                          │ ls/signal/mlp_model.py           │
 └─────┴───────────────────────────┴──────────────────────────────────────────┴──────────────────────────────────┘

 Phase 3: 关键功能补齐（12 项）

 ┌──────┬───────────────────────────────┬──────────────────────────────────┬──────────────────────────────────────┐
 │  ID  │             问题              │               文件               │               修复方式               │
 ├──────┼───────────────────────────────┼──────────────────────────────────┼──────────────────────────────────────┤
 │ P3.1 │ SHAP 分析完全未实现           │ layers/evaluation/alpha_eval.py  │ 添加 compute_shap() 方法             │
 ├──────┼───────────────────────────────┼──────────────────────────────────┼──────────────────────────────────────┤
 │ P3.2 │ 归因分析三方法全是占位符      │ layers/evaluation/attribution.py │ 从BacktestEngine                     │
 │      │                               │                                  │ trades中提取cost，实现真实分解       │
 ├──────┼───────────────────────────────┼──────────────────────────────────┼──────────────────────────────────────┤
 │ P3.3 │ 全流程管线 RL 步骤是占位符    │ frontend/pages/11_全流程管线.py  │ 接入 v3 RLTrainer                    │
 ├──────┼───────────────────────────────┼──────────────────────────────────┼──────────────────────────────────────┤
 │ P3.4 │ YAML ensemble.method          │ config/model.yaml                │ 统一命名（weighted/stacking/regime） │
 │      │ 值与前端选项不匹配            │                                  │                                      │
 ├──────┼───────────────────────────────┼──────────────────────────────────┼──────────────────────────────────────┤
 │ P3.5 │ Linear/Ridge/Lasso缺少lr      │ DL模型文件                       │ 添加ReduceLROnPlateau +              │
 │      │ scheduler和gradient clipping  │                                  │ clip_grad_norm_                      │
 ├──────┼───────────────────────────────┼──────────────────────────────────┼──────────────────────────────────────┤
 │ P3.6 │ DL序列模型 predict()          │ LSTM/GRU/TCN/Transformer         │ 改为 raise ValueError 或 log warning │
 │      │ 短序列静默返回零              │                                  │                                      │
 ├──────┼───────────────────────────────┼──────────────────────────────────┼──────────────────────────────────────┤
 │ P3.7 │ DL模型 save/load 使用错误的   │ LSTM/GRU/TCN/Transformer/MLP     │ 覆盖 save/load 方法                  │
 │      │ pickle（应为 torch.save）     │                                  │                                      │
 ├──────┼───────────────────────────────┼──────────────────────────────────┼──────────────────────────────────────┤
 │ P3.8 │ XGBoost/LightGBM 硬编码       │ xgboost_model/lightgbm_model     │ 从 config 读取 objective             │
 │      │ objective                     │                                  │                                      │
 ├──────┼───────────────────────────────┼──────────────────────────────────┼──────────────────────────────────────┤
 │ P3.9 │ 事件队列按插入顺序而非timesta │ layers/backtest/event_queue.py   │ 改为按 event.timestamp 排序          │
 │      │ mp排序                        │                                  │                                      │
 ├──────┼───────────────────────────────┼──────────────────────────────────┼──────────────────────────────────────┤
 │ P3.1 │ 分类模式 get_target 只返回    │ layers/signals/labels/label_fact │ 返回三分类 -1/0/1 标签               │
 │ 0    │ label_up                      │ ory.py                           │                                      │
 ├──────┼───────────────────────────────┼──────────────────────────────────┼──────────────────────────────────────┤
 │ P3.1 │ ModelTrainer未实现统一训练循  │ layers/signals/trainer.py        │ 重构为真正包含epoch循环+早停+lr调度  │
 │ 1    │ 环                            │                                  │ 的训练器                             │
 ├──────┼───────────────────────────────┼──────────────────────────────────┼──────────────────────────────────────┤
 │ P3.1 │ BacktestEngine未集成          │ layers/backtest/backtest_engine. │ 用 ExecutionSimulator                │
 │ 2    │ ExecutionSimulator            │ py                               │ 替换内联交易执行                     │
 └──────┴───────────────────────────────┴──────────────────────────────────┴──────────────────────────────────────┘

 Phase 4: 清理与质量提升

 - models/__init__.py 导出不全 — 补充所有模型导出
 - 删除 layers/models/ 下剩余的旧目录
 - 旧 v2 脚本引用的 layers 移除后清理空目录
 - 6_模型训练.py 中 MLPWrap 内联类改为使用新的 v3 MLP 模型
 - layers/portfolio/optimizer.py — current_weights 参数被忽略问题
 - layers/backtest/event.py — 补充 StopLossEvent 类
 - layers/features/feature_cache.py — 添加 TTL 过期策略
 - log return 指标缺失 — 补充到 trend_features

 删除后需要修改的文件

 ┌──────────────────────────────────┬───────────────────────────────────────────────────────────────────────────────┐
 │             修改文件             │                                     改动                                      │
 ├──────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────┤
 │ predictor.py                     │ 将 from data_engine import DataEngine 改为 from improved_data_engine import   │
 │                                  │ DataEngine                                                                    │
 ├──────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────┤
 │ frontend/pages/6_模型训练.py     │ 将 MLP 导入从 layers.models.signal.mlp_model 改为                             │
 │                                  │ layers.signals.models.mlp_model（Phase 2.8 后）                               │
 ├──────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────┤
 │ layers/models/signal/__init__.py │ 删除此文件                                                                    │
 ├──────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────┤
 │ layers/models/__init__.py        │ 清理导出                                                                      │
 ├──────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────┤
 │ diagnose.py                      │ 移除 rl_agent 和 from data_engine import 相关诊断代码                         │
 ├──────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────┤
 │ run.py                           │ 移除对 data_engine.py 的检查，移除对 3_AI交易 的引用                          │
 ├──────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────┤
 │ stock_data_manager.py            │ 检查并移除对已删模块的引用                                                    │
 └──────────────────────────────────┴───────────────────────────────────────────────────────────────────────────────┘

 预计工作量

 ┌─────────────────────┬────────┬────────────┐
 │        Phase        │ 项目数 │  预计时间  │
 ├─────────────────────┼────────┼────────────┤
 │ Phase 1（崩溃修复） │ 5      │ 30 min     │
 ├─────────────────────┼────────┼────────────┤
 │ Phase 2（架构修复） │ 8      │ 2-3 hours  │
 ├─────────────────────┼────────┼────────────┤
 │ Phase 3（功能补齐） │ 12     │ 3-4 hours  │
 ├─────────────────────┼────────┼────────────┤
 │ Phase 4（清理收尾） │ 10+    │ 1-2 hours  │
 ├─────────────────────┼────────┼────────────┤
 │ 总计                │ 35+    │ 7-10 hours │
 └─────────────────────┴────────┴────────────┘