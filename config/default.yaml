# ===========================
# 系统默认配置
# ===========================

system:
  name: "InstitutionalQuant"
  version: "3.0.0"
  seed: 42
  output_dir: "results"
  log_dir: "logs"
  model_dir: "models"
  data_dir: "stock_data"

# 开发阶段: single_asset | multi_asset | live
stage: "single_asset"

# 当前分析的股票
ticker: "AAPL"
benchmark: "SPY"
start_date: "2015-01-01"
